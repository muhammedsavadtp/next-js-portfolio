export default interface types {
  children: React.ReactNode;
  name: string;
  description: string;
  fields: { [key: string]: any };
  methods: { [key: string]: any };
  url: string;
  heading: string;
  paragraph: string;
  e: any;
  menuItems: any;
  isDarkMode: boolean;
  title: string;
  image: string;
  items: any;
  data:any
}